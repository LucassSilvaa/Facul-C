#include <stdio.h>
#include <math.h>
int main(){
    int cont, soma = 0;
    for (cont = 1; cont <= 100; cont++) {
      soma+= cont;
    
    }printf("O resultado da soma é : %d", soma);

    return 0;
}

